# RunPanel Configuration
# Copy this file to .env.local and adjust values

# Secret key for encrypting env vars and session tokens (auto-generated on first run if empty)
RUNPANEL_SECRET=

# Data directory (default: ./data)
RUNPANEL_DATA_DIR=

# Panel port (default: 3000)
PORT=3000
